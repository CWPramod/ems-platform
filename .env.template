# ═══════════════════════════════════════════════════════════════════════════════
# EMS Platform — Production Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in all values before deployment:
#   cp .env.template .env
#
# NEVER commit the .env file to version control!
#

# ─── Database (PostgreSQL) ────────────────────────────────────────────────────
# PostgreSQL database name
DATABASE_NAME=ems_platform

# PostgreSQL admin username
DATABASE_USER=ems_admin

# PostgreSQL password — CHANGE THIS to a strong random value
DATABASE_PASSWORD=CHANGE_ME_strong_random_password

# ─── Security (REQUIRED) ─────────────────────────────────────────────────────
# JWT signing secret for API authentication (minimum 32 characters)
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_48

# License module signing secret (minimum 32 characters)
# Generate with: openssl rand -base64 48
LICENSE_SIGNING_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_48

# ITSM module API key for inter-service authentication
ITSM_MODULE_API_KEY=CHANGE_ME_itsm_api_key

# ─── Data Mode ────────────────────────────────────────────────────────────────
# 'production' — only real data from SNMP/monitoring
# 'demo'       — generates simulated events/alerts for demonstrations
DATA_MODE=production

# ─── SNMP Configuration ──────────────────────────────────────────────────────
# SNMP operating mode: 'production' for real devices, 'simulation' for testing
SNMP_MODE=production

# Default SNMP community string for device discovery
SNMP_COMMUNITY=public

# Enable/disable NMS background polling (true/false)
NMS_POLLING_ENABLED=true

# Comma-separated CIDR subnets for auto-discovery (optional)
# Example: 10.0.1.0/24,192.168.1.0/24
DISCOVERY_SUBNETS=

# Comma-separated static IPs for targeted discovery (optional, alternative to subnets)
# Use for known device IPs that don't need subnet scanning
# Example: 10.0.1.1,10.0.1.2,192.168.1.1
DISCOVERY_IPS=

# ─── Ports ────────────────────────────────────────────────────────────────────
# Frontend web port (exposed to clients)
WEB_PORT=80

# ─── Logging ──────────────────────────────────────────────────────────────────
# Application log level: debug, info, warn, error
# LOG_LEVEL=info
