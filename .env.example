# EMS Platform Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ─── Application ────────────────────────────────────────────────────────────
NODE_ENV=development
API_PORT=3100
WEB_PORT=80

# ─── Database (PostgreSQL) ──────────────────────────────────────────────────
DATABASE_HOST=localhost
DATABASE_PORT=5433
DATABASE_NAME=ems_platform
DATABASE_USER=ems_admin
DATABASE_PASSWORD=your_secure_password_here

# ─── Security (REQUIRED - Generate strong random values!) ───────────────────
# JWT secret for authentication tokens (min 32 characters)
JWT_SECRET=your_jwt_secret_min_32_characters_here

# License signing secret (min 32 characters)
LICENSE_SIGNING_SECRET=your_license_signing_secret_min_32_chars

# ─── Data Mode ────────────────────────────────────────────────────────────────
# Controls whether simulation services generate demo data
# Options: 'demo' (default) - generates fake events/alerts for testing
#          'production' - only real data from SNMP/monitoring, no simulated events
DATA_MODE=demo

# ─── SNMP Monitoring ────────────────────────────────────────────────────────
# Options: 'simulation' (default) or 'production'
SNMP_MODE=simulation

# ─── Optional: Rate Limiting ────────────────────────────────────────────────
# THROTTLE_TTL=60
# THROTTLE_LIMIT=100

# ─── Optional: CORS ─────────────────────────────────────────────────────────
# CORS_ORIGIN=http://localhost:5173

# ─── Optional: Logging ──────────────────────────────────────────────────────
# LOG_LEVEL=debug

# ─── Docker Compose ─────────────────────────────────────────────────────────
# These are used by docker-compose.yml
# COMPOSE_PROJECT_NAME=ems-platform
